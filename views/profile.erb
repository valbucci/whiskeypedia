<% if @Userz %>
	<header>
		<h2>Your Profile</h2>
		<h3><%= @Userz.username %></h3>
	</header>
<% end %>

<section>
	<%
		@his.each do |his|
	%>
		<div class="historyLog">
			<a href="/history/<%= his[:id] %>" class="editHistLink">
				<%= his[:date] %>
			</a>
		</div>
	<% end %>
</section>
